%% %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%           EXAMPLE 3            %
%% %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
clc
clear
close all

% Find transformation matrix for counter-clockwise rotation by 60 deg
% around x axis and and l=50mm.

%%

q =  ;          % rotation angle
l =  ;          % link length

% Abbreviations
cq = cos(q) ;
sq = sin(q) ;

% Rotation Matrix for rotation around x axis
Rx = [  ;
        ;
        ] ;
 
%% 
